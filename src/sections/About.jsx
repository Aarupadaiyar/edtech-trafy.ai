export default function About() {
  return (
    <section className="py-16 md:py-20 border-b border-[var(--ink-line)]">
      <div className="container-x max-w-3xl">
        <div className="eyebrow mb-4">Our Mission</div>
        <p className="font-display text-2xl md:text-3xl leading-[1.3] text-[var(--bone)]">
          Employability is built through execution, not certificates.
          <span className="text-[var(--mist)]"> We put learners in front of real, shipped AI products from day one.</span>
        </p>
      </div>
    </section>
  );
}
