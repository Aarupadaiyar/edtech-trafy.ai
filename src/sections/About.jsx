export default function About() {
  return (
    <section className="py-24 md:py-32 border-b border-[var(--ink-line)]">
      <div className="container-x max-w-4xl">
        <div className="eyebrow mb-6">Our Mission</div>
        <p className="font-display text-3xl md:text-5xl leading-[1.25] text-[var(--bone)]">
          We believe employability is built through execution, not certificates.
          <span className="text-[var(--mist)]"> Trafy AI exists to close the gap between education and industry —
          by putting learners in front of real AI products, from day one.</span>
        </p>
      </div>
    </section>
  );
}
